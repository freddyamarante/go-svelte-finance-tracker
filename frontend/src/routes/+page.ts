import type { PageLoad } from './$types';

export const load: PageLoad = async () => {
	// This page load function ensures the page is properly loaded
	// The authentication logic is handled in the layout
	return {};
}; 